<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet">
    <link rel="shortcut icon" href="/images/TIT-logo.webp" type="image/x-icon">
    <style>
        body::-webkit-scrollbar{
            display: none;
        }
    </style>
</head>
<body class="bg-gray-200 md:flex">
    
    <div class="fixed top-0 w-full p-2 md:p-4 font-bold bg-green-600 text-white">
        <h1 class="text-center text-xl">All Users</h1>
    </div>    

    <div class="ml-0 p-1 mt-10 sm:mt-14 sm:p-0 mb-14  flex flex-col items-center w-full justify-center">
      <% if (users && users.length > 0) { %>
      <table class="w-full bg-white shadow-md rounded mb-4">
          <thead >
              <tr class="bg-gray-300 text-gray-600 uppercase text-sm leading-normal">
                  <th class="py-4 px-1 lg:px-10 text-center">S.N</th>
                  <th class="py-4 px-1 bg-yellow-300 text-black-600 lg:px-10 text-center">Show</th>
                  <th class="py-4 px-1 lg:px-10 text-center">Name or Email</th>
                  <th class="py-4 px-1 hidden lg:table-cell lg:px-10 text-center">Mobile Number</th>
                  <th class="py-4 px-1 hidden lg:table-cell lg:px-10 text-center">Update</th>
                  <th class="py-4 px-1 hidden lg:table-cell lg:px-10 text-center">Delete</th>
              </tr>
          </thead>
          <tbody class="text-gray-600 text-sm font-light">
              <% users.forEach((user, index) => { %>
              <tr class="border-b border-gray-200 hover:bg-gray-100 text-center">
                  <td class="py-4 px-1 lg:px-16 text-center"><%= index + 1 %></td>
                  <td class="py-4 px-1 lg:px-16 text-center">
                    <a href="/feed/user/Dets/<%= user._id %>" class="flex justify-center items-center h-full">
                      <img class="w-8 h-8 rounded-full overflow-hidden shadow-sm bg-gray-200" src="<%= user.img %>" alt="">
                    </a>
                  </td>                  
                  <td class="py-4 px-1 lg:px-10 text-center"><%= user.username %></td>
                  <td class="py-4 px-1 hidden lg:table-cell lg:px-12 text-center">+91 <%= user.mobNumber%></td>
                  <td class="py-4 px-1 hidden lg:table-cell lg:px-16 text-center"><a href="/feed/user/update/<%= user._id %>"><i class="ri-edit-line text-blue-500 bg-indigo-100 p-1 rounded-full"></i></a></td>
                  <td class="py-4 px-1 hidden lg:table-cell lg:px-16 text-center"><a href="/feed/user/delete/<%= user._id %>"><i class="ri-delete-bin-6-line text-red-500 bg-red-100 p-1 rounded-full"></i></a></td>
              </tr>
              <% }) %>
          </tbody>
      </table>
      <% } else { %>
        <div class="mx-auto flex flex-col items-center justify-center h-screen">
            <h1 class="p-2 bg-red-700 rounded border border-slate-500 text-center text-white font-bold">No Student Available</h1>
            <a href="/feed/admin/createNewUser" class="text-center flex flex-col items-center justify-center mt-7">
                <i class="ri-user-add-fill w-12 h-12 rounded-full bg-green-700 text-white text-2xl flex items-center justify-center"></i>
                <p class="font-bold">Add new Student</p>
            </a>
        </div>        
      <% } %>
    </div>

    <footer class="w-full bg-green-400 fixed bottom-0 z-10 flex justify-between items-center px-8 py-2 ">
        <div class="flex flex-col items-center py-1 px-3 rounded-full cursor-pointer">
            <a href="/feed/admin/createNewUser">
                <div class="w-6 h-6 bg-gray-200 rounded-full overflow-hidden shadow-lg"><i class="ri-user-add-line flex center items-center justify-center"></i></div>
            </a>
            <p class="text-xs font-semibold text-black-300">Add</p>
        </div>          
        <div class="flex flex-col  items-center py-1 px-3 rounded-full cursor-pointer">
            <a href="/feed/admin/Dets/<%= admin._id %>">
                <div class="w-6 h-6 bg-gray-200 rounded-full overflow-hidden shadow-lg"><i class="ri-slideshow-view flex center items-center justify-center"></i></div>
            </a>
            <p class="text-xs font-semibold text-black-300 ">Admision</p>
        </div>
        <div class="flex flex-col items-center py-1 px-3 rounded-full cursor-pointer">
            <a href='/feed/admin/Dets/<%= admin._id %>'>
                <img class="w-6 h-6 bg-gray-200 rounded-full overflow-hidden shadow-lg" src="<%= admin.img %>" alt="Profile Image" />
            </a>
            <p class="text-xs font-semibold text-black-300 "><%= admin.username %></p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</body>

</html>
