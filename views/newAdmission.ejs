<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admission Inquiry Feed</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"/>
  <style>
    .header-title {
      color: #fff;
    }
    .content-card {
      background-color: #F8F9FA;
      border-radius: 20px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    }
    .card-header {
      background-color: #EDF2F7;
      border-top-left-radius: 20px;
      border-top-right-radius: 20px;
    }
    #cart{
      width: 100%; 
      max-height: 450px; 
      margin: 10px;
      max-width: 400px; 
    }
  </style>
</head>
<body class="bg-gray-100">
  <header class="w-full p-1 py-2 bg-green-900 fixed top-0">
    <div class="container mx-auto px-4">
      <h1 class="text-3xl font-semibold header-title text-center">All New Admission Request.</h1>
    </div>
  </header>
  <div class="flex flex-wrap w-full justify-center items-center mt-16" id="mainDiv">
   <% if (newAdmissions.length > 0) { %>
    <% newAdmissions.forEach(user => { %>
      <div id="cart" class="w-full md:w-1/2 lg:w-1/3 xl:w-1/4 "> 
        <div class="max-w-md mx-auto content-card h-full "> 
          <div class="card-header py-4 px-4 flex items-center justify-center">
            <img class="w-20 h-20 rounded-full border border-black" src="<%= user.image  %>" alt="photo">
          </div>
          <div class="card-body py-6 px-6 text-black">
            <p><b>Name : <%= user.name %></b></p>
            <p><b>Email</b> : <%= user.email %></p>
            <p><b>Mob Number</b> : <%= user.phoneNumber %></p>
            <p><b>Course </b> : <%= user.course %></p>
            <p class="mt-1 mb-1"><b>Message</b></p>
            <div class=" overflow-x-auto max-h-32" id="scroller"> 
              <p><%= user.message %></p>
            </div>
          </div>
          <a href="/feed/admin/delete/request/<%= user._id %>" class="flex justify-center "><i class="ri-delete-bin-4-line text-white px-1 bg-red-700 rounded-full"></i></a>
        </div>
      </div>
    <% }) %>
   <% } else {%>
    <div class="w-full md:w-1/2 lg:w-1/3 xl:w-1/4 p-4">
      <div class="max-w-md mx-auto content-card h-full"> 
        <div class="card-header py-4 px-6">
          <h2 class="text-xl font-semibold">Latest Inquiry</h2>
        </div>
        <div class="card-body py-6 px-6">
          <p class="text-lg">No admission inquiries yet.</p>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</body>
</html>
